"use strict" // treat all JS code as newer version

let name = "Pratik" // String
let age = 27 // number
let isLogIn = true // boolean
let height; // undefine
let phoneNumber = null; //null
let size = BigInt("9999999999999999"); //BigInt -> an integer with arbitrary precision
let size1 = 9999999999999999n; //BigInt -> an integer with arbitrary precision
let specialSymbole = "!@#$%^&*()_+"; //a data type whose instances are unique and immutable. To find unique ness.

let person = {
    name: "Pratik",
    age: 30,
    gender: "Male"
} // Object

// typeOf to find what type of variable it is.

console.log(typeof(person));